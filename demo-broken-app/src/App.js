import React from 'react'

// Intentionally buggy demo app — contains a few issues for testing
export default function App(){
  // Deliberate runtime error: undefined variable
  // Also some other small issues intentionally left in place
  return (
    <div>
      <h1>Demo Broken App</h1>
      <p>This app contains intentional bugs for the RepoGuardian demo.</p>
    </div>
  )
}
